# Changelog — Linkiir Notification Adapters

Every published change to this catalog, newest first. Library versions are immutable: a library fix appears here as a new version, never as an edit to an existing one.

## Unreleased

- Added repository documentation (README, changelog, docs skeleton). No adapter or library content changed.
